import 'package:flutter/material.dart';

class flower_pot extends StatelessWidget {
  const flower_pot({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
