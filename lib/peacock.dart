import 'package:flutter/material.dart';

class peacock extends StatelessWidget {
  const peacock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
