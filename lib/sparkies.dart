import 'package:flutter/material.dart';

class sparkies extends StatelessWidget {
  const sparkies({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
